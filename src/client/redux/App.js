const React = require('react')
const CounterContainer = require('./containers/Counter/CounterContainer')

const App = () => (
  <div id="app">
    <CounterContainer />
  </div>
)

module.exports = App